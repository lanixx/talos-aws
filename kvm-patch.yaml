machine:
  kubelet:
    extraArgs:
      rotate-server-certificates: "false"
  nodeLabels:
    node-role.kubernetes.io/worker: ""
  kernel:
    modules:
      - name: kvm
      - name: kvm_intel
        parameters:
          - nested=1
          - ept=1